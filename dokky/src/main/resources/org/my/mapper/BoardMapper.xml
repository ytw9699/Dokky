<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.my.mapper.BoardMapper">

	<select id="getList" resultType="org.my.domain.BoardVO">
	<![CDATA[
		select * from DK_BOARD where kind = #{kind} and num > 0
		]]>
	</select>

	<!-- 입력+pk값을 알기 위한것 -->
	<insert id="insertSelectKey">

		<selectKey keyProperty="NUM" order="BEFORE" resultType="long">
			select seq_dk_board.nextval from dual
		</selectKey>

		insert into dk_board (NUM,KIND,TITLE,CONTENT, NICKNAME)
		values (#{NUM},#{KIND},#{TITLE}, #{CONTENT}, #{NICKNAME})
		
	</insert>
	
</mapper>
