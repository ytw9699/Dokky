<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.my.mapper.BoardMapper">

	<select id="getList" resultType="org.my.domain.BoardVO">
		<![CDATA[ select * from DK_BOARD where category = #{category} and num > 0 ]]>
	</select>

	<!-- 입력+pk값을 알기 위한것 -->
	<insert id="insertSelectKey">

		<selectKey keyProperty="num" order="BEFORE" resultType="long">
			select seq_dk_board.nextval from dual
		</selectKey>

		insert into DK_BOARD (num,category,title,content,nickName)
		values (#{num},#{category},#{title}, #{content}, #{nickName})
		
	</insert>
	
	<select id="read" resultType="org.my.domain.BoardVO">
		select * from DK_BOARD where num = #{num}
	</select>
	
	<update id="update">
		update DK_BOARD
			set title= #{title},
			content=#{content},
			updateDate = sysdate
		where num = #{num}
	</update>
	
	<delete id="delete">
		delete DK_BOARD where num = #{num}
	</delete>
	
</mapper>
