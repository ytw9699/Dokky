<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.my.mapper.BoardMapper">
	
	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item='type' collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'C'.toString()">
							content like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'W'.toString()">
							writer like '%'||#{keyword}||'%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<select id="getListWithPaging" resultType="org.my.domain.BoardVO"> 
		<![CDATA[ select category,replyCnt, num, title, content, nickName, regdate, hitCnt from ( 
		select /*+INDEX_DESC(DK_BOARD PK_DK_BOARD) */ rownum rn, category,replyCnt, num, title, content, 
		nickName, regdate, hitCnt from DK_BOARD where category = #{category} and rownum <= #{pageNum} * #{amount} 
		) where rn > (#{pageNum} -1) * #{amount} ]]> </select>   
		
	<!-- 입력+pk값을 알기 위한것 -->
	<insert id="insertSelectKey">

		<selectKey keyProperty="num" order="BEFORE" resultType="long">
			select seq_dk_board.nextval from dual
		</selectKey>

		insert into DK_BOARD (num,category,title,content,nickName)
		values (#{num},#{category},#{title}, #{content}, #{nickName})
		
	</insert>
	
	<select id="read" resultType="org.my.domain.BoardVO">
		select * from DK_BOARD where num = #{num}
	</select>
	
	<update id="updateHitCnt">
		update dk_board set hitCnt = hitCnt+1 where num = #{num}
	</update>
	
	<update id="update">
		update DK_BOARD
			set title= #{title},
			content=#{content},
			updateDate = sysdate
		where num = #{num}
	</update>
	
	<delete id="delete">
		delete DK_BOARD where num = #{num}
	</delete>
	
	<select id="getTotalCount" resultType="int">
		select count(*) from DK_BOARD where category = #{category} and num >0
	</select>
	
	
	
	
		
</mapper>
